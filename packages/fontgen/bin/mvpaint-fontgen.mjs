#!/usr/bin/env node
// The published command. Everything it does is in dist/main.js, which runs on import; this file
// exists to carry the shebang and the name npm links onto PATH.
import '../dist/main.js'
